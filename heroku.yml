build:
  docker:
    web: Dockerfile.web
    worker: Dockerfile.worker
run:
  web: uvicorn fastapi_app.main:app --host 0.0.0.0 --port $PORT
  worker: bokeh serve --port=$PORT --address=0.0.0.0 --allow-websocket-origin=* --use-xheaders bokeh_app